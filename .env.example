# ==============================================================================
# БЕЗОПАСНОСТЬ: Скопируйте этот шаблон в локальный файл .env командой:
#   cp .env.example .env
# Заполните реальные ключи в .env. Файл .env добавлен в .gitignore и никогда
# не должен попадать в git-репозиторий!
# ==============================================================================

# Groq — облачный диалог и аналитика ИИ (обязательно для навыка ИИ и авто-аналитики)
GROQ_API_KEY=
# Рекомендуемая быстрая модель: openai/gpt-oss-20b или qwen/qwen3.6-27b
GROQ_MODEL=openai/gpt-oss-20b

# T-Invest API — котировки, портфель, сделки (навык «Биржа и портфель»)
# ВНИМАНИЕ: живой токен с правом торговли = реальные заявки,
# только если включены сделки голосом или автоторговля.
# Сделки из Джарвиса: тумблер «Сделки голосом» или TINKOFF_VOICE_TRADE=true
# (пустое значение и нет ключа в JSON — выкл; сводка голосом остаётся).
# Фоновые заявки: TINKOFF_AUTO_TRADE=true (пустое значение — только песочница).
# Для тестов: TINKOFF_SANDBOX=true
# TINKOFF_TOKEN или TINKOFF_INVEST_TOKEN: токен брокерского API
# (чтение — сводка; торговля — права на заявки)
TINKOFF_TOKEN=
# TINKOFF_ACCOUNT_ID: ID конкретного счёта (опционально; если пусто — определится автоматически)
TINKOFF_ACCOUNT_ID=
# TINKOFF_SANDBOX: песочница без реальных денег (true / false)
TINKOFF_SANDBOX=false
# TINKOFF_VOICE_TRADE: заявки по команде («купи», «продай», «поторгуй»).
# true / false. Если пусто — выкл. Тумблер пишет stocks_voice_trade в JSON
# и при следующем старте этот ключ важнее пустого значения здесь.
TINKOFF_VOICE_TRADE=
# TINKOFF_AUTO_TRADE: фоновые заявки раз в ~45 мин.
# true / false. Если пусто — включено только в песочнице.
# Тумблер в окне настроек пишет stocks_auto_trade в skills_enabled.json
# и при следующем старте этот ключ важнее пустого значения здесь.
TINKOFF_AUTO_TRADE=
# TINKOFF_INVEST_TOKEN: то же, что TINKOFF_TOKEN (алиас)
# TINKOFF_CA_BUNDLE: свой PEM, иначе certs/russian_trusted_root_ca.pem (Минцифры)
# TINKOFF_WATCHLIST: тикеры наблюдения через запятую, если токена нет
TINKOFF_WATCHLIST=SBER,LKOH,YDEX,VTBR

# Telegram-бот: входящие команды и уведомления охраны
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Охрана: индекс камеры OpenCV или /dev/videoN (по умолчанию 0)
# CAMERA_INDEX=0

# Голос синтеза Piper TTS: ru_RU-dmitri-medium.onnx (мужской голос Дмитрия), ru_RU-ruslan-medium.onnx, ru_RU-irina-medium.onnx
PIPER_MODEL=ru_RU-dmitri-medium.onnx
VOICE_SPEED=1.0

# Погода по умолчанию: город (геокодинг) и/или точные координаты.
# Если заданы DEFAULT_LAT и DEFAULT_LON, «какая погода» без города идёт в эту точку.
# DEFAULT_CITY тогда только имя вслух; пустое имя озвучивается как «здесь».
# Алиасы: DEFAULT_LATITUDE / DEFAULT_LONGITUDE
DEFAULT_CITY=Москва
# DEFAULT_LAT=
# DEFAULT_LON=

# Лампа Xiaomi / Yeelight (опционально)
XIAOMI_BULB_IP=
XIAOMI_BULB_TOKEN=

# Home Assistant (навык «умный дом», тумблер в настройках)
# HAOS в GNOME Boxes проброшен на localhost:8123
HA_URL=http://127.0.0.1:8123
HA_TOKEN=
# HOMEASSISTANT_TOKEN: то же, что HA_TOKEN (алиас)

# Сессия внимания (в секундах) в тишине и при играющей музыке
ATTENTION_TIMEOUT=12
ATTENTION_TIMEOUT_MUSIC=5

# Настройки приглушения музыки (Ducking) и фильтрации эхо колонок
# DUCKING_VOLUME: уровень звука Audacious при диалоге в % (по умолчанию 8)
DUCKING_VOLUME=8
# DUCKING_MODE: duck (приглушать) или pause (ставить плеер на паузу)
DUCKING_MODE=duck
# MIN_SPEECH_RMS: минимальная энергия голоса для отсечения фонового пения колонок (200 = оптимум, 0 = выкл)
MIN_SPEECH_RMS=200
# AUDIO_INPUT_DEVICE: индекс устройства sounddevice или имя источника (опционально, по умолчанию default)
# AUDIO_INPUT_DEVICE=

# Поиск и карты: yandex или google
SEARCH_PROVIDER=yandex
MAPS_PROVIDER=yandex

# Браузерный ИИ по команде «открой нейросеть»: yandex (Алиса) или gemini
AI_PROVIDER=yandex
# CHROME_PATH: полный путь к chrome/chromium, если не в PATH

# Консольный режим и отображение GUI
# CONSOLE_MODE: запускать ассистент в текстовом терминале по умолчанию (true / false)
CONSOLE_MODE=false
# GUI_ENABLED: отображать плавающий виджет-сферу (true / false)
GUI_ENABLED=true
